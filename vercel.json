# FORM · Body Lab — 部署指南

## 第一步：建立 Supabase 數據庫（免費）

1. 前往 [supabase.com](https://supabase.com) → 註冊 → New Project
2. 記下 **Project URL** 和 **anon public key**（在 Settings → API）
3. 進入 **SQL Editor**，貼上以下 SQL 並執行：

```sql
-- 記憶庫表
CREATE TABLE memories (
  id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
  content text NOT NULL,
  tier text NOT NULL CHECK (tier IN ('perm','mid','short','day')),
  expires_at timestamptz,
  created_at timestamptz DEFAULT now()
);

-- 開啟 RLS（Row Level Security）但允許匿名讀寫
ALTER TABLE memories ENABLE ROW LEVEL SECURITY;
CREATE POLICY "allow all" ON memories FOR ALL USING (true) WITH CHECK (true);
```

---

## 第二步：上傳到 GitHub

1. 登入 [github.com](https://github.com) → New repository
2. 名稱設為 `form-bodylab`，Public，**不要**勾選 Initialize README
3. 上傳以下文件結構：

```
form-bodylab/
├── vercel.json
└── public/
    ├── index.html
    ├── manifest.json
    ├── icon-192.svg
    └── icon-512.svg
```

> 在 GitHub 網頁上點 **Add file → Upload files**，把這些文件拖進去即可。

---

## 第三步：部署到 Vercel（免費）

1. 前往 [vercel.com](https://vercel.com) → 用 GitHub 帳號登入
2. **New Project** → 選擇 `form-bodylab` repo
3. Framework Preset 選 **Other**
4. **Root Directory** 填 `public`
5. 點 **Deploy** → 等待 1 分鐘

部署完成後你會得到一個網址，例如：`https://form-bodylab-xxx.vercel.app`

---

## 第四步：加到 iPhone 主屏幕

1. 用 Safari 打開你的 Vercel 網址
2. 點底部分享按鈕 →「加入主畫面」
3. 名稱改為 **FORM**，點「新增」

完成！圖標出現在主屏幕，全屏打開，和原生 App 一樣。

---

## 第五步：首次設定

打開 App → 輸入：
- **Anthropic API Key**：從 [console.anthropic.com](https://console.anthropic.com) 獲取
- **Supabase URL**：第一步記下的 Project URL
- **Supabase Anon Key**：第一步記下的 anon public key

設定後 Keys 存在本地，不會離開你的設備。

---

## 功能說明

| 功能 | 說明 |
|------|------|
| 飲食記錄 | 輸入文字 → AI 自動解析宏量素，點 × 可撤回 |
| 訓練計劃 | AI 讀取記憶庫後生成個人化計劃 |
| 記憶庫 | 四層時效：永久 / 中期 / 短期 / 當日，跨設備同步 |
| 身體數據 | 圍度、成分追蹤 + 進化軌跡圖 |

## 費用參考

| 服務 | 費用 |
|------|------|
| Vercel | 免費 |
| Supabase | 免費（500MB 數據庫，完全夠用） |
| Anthropic API | 按用量，日常使用約 $1-3 USD/月 |
